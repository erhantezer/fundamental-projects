import React, { useEffect } from 'react'

const useFetch = () => {

    const getProducts = () => {
        try {
            
        } catch (error) {
            
        }
    };

    useEffect(() => {
        getProducts()
    },[])

    return {}
}

export default useFetch